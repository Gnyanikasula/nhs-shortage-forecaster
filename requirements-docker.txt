# Trimmed specifically for the Docker container -- the full requirements.txt
# (used for local dev in the venv) includes Jupyter and matplotlib, neither
# of which a headless scheduled scoring job needs. MLflow IS included here
# (unlike the original trim) because score_and_log() now logs production
# retrain metadata to it on every real run -- this is not optional weight,
# it's a runtime dependency. streamlit removed -- dropped in favor of the
# real HTML/CSS/JS frontend served by FastAPI.
pandas==2.2.3
numpy==1.26.4
openpyxl==3.1.5
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0
pyarrow==17.0.0
scikit-learn==1.5.2
xgboost==2.1.2
joblib==1.4.2
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
fastapi==0.115.0
uvicorn==0.32.0
mlflow==2.17.2